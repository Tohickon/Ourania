# HANDOVER — pointer

**This is not the handover. The live one is in the vault:**

`C:\Users\daver\Projects\BRAIN\Brain\HANDOVER.md`

(`C:\Users\daver\Documents\BRAIN\Brain\HANDOVER.md` is the same file, through a directory junction.)

Read that first and update that last. Every change since 2026-09-12 is recorded there as its own
note in `Resources/sessions/`, linked from its Update log.

## Why this file is only a pointer

Until 2026-09-19 this file held its own handover, and it had not been kept since 28 August — it
still reported 22 suites and 170,648 checks when there were 55 suites and over 600,000. An agent
that read the repository first worked from that picture: on 2026-09-16 it built the PDF report
without knowing anything that had happened in three weeks, and recorded its work here, where
nobody else looked. Two handovers is the defect the project keeps naming — one fact kept in two
places, and the copies drifting apart — so there is now one, and this file only says where it is.

The old contents are in git history (last committed version: `7a227621`).
